import { Container, ListItem, UnorderedList, Text } from "@chakra-ui/react";

const Tooling = () => {
  return (
    <Container>
      <Text>Who am I?</Text>
    </Container>
  );
};
export default Tooling;
